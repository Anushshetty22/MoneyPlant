import type { NextConfig } from "next";

// NextConfig is kept explicit so future image, rewrite, or environment settings
// have one documented place instead of being scattered through app components.
const nextConfig: NextConfig = {
  /* Phase 7.1 foundation: no custom Next.js behavior is required yet. */
};

export default nextConfig;
